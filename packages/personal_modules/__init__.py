"""Account-owned Operly capabilities and connectors.

Personal modules are rooted at the authenticated human, never at a Workspace tenant.
They compose into the same Kernel contracts used by Workspace tools so a future agent
can discover and execute them through one stable capability protocol.
"""
