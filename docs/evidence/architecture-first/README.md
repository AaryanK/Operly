# Architecture-first browser evidence

Open `index.html` for the local HTML evidence package. No production data or credentials were used.

Binary PNG export is intentionally absent: the browser states were visually verified, but the available interface could not persist screenshots. The index and root milestone evidence document contain the complete deferred capture manifest.
