# Unified Agent Runtime

This document is intentionally created early on the migration branch so implementation changes can reference one security invariant: every human, workflow, generated solution, Studio surface, and delegated agent uses the same governed capability fabric; generated code never receives provider credentials and temporary sandbox execution never becomes durable authority or storage.
